/* Node.js

Es un entorno de código abierto, multiplataforma que permite crear apps del lado del servidor con JS, por ejemplo: APIs, WEB apps e incluso acceder a archivos y leer sus contenidos, realizar pagos de forma segura, autentificar usuarios, subir archivos, consultar a DDBB.
Tiene una gran cantidad de librerias, soporte, ejemplos, documentación y comunidad.

NPM (Node Package Manager): una gran cantidad de paquetes y librerías de código abierto listos para ser utilizados. 

YARN

Package.json {
  dependencies: dependencias de producción (cuando el proyecto ya este publicado),
  devDependencies: dependencias de desarrollo (cuando se está creando el proyecto),
}

Package.lock nunca debe ser modificado.

Express: Framework más popular de Node.


Librerías(dependencias) {
  nodemon: detecta cambios en los archivos y reiniciar el servidor(en desarrollo),
  express: (),
}

*/